name: nasdaq-bb-qqq-len60-k2-logclose

on:
  workflow_dispatch: {}
  schedule:
    # 22:40 Asia/Taipei = 14:40 UTC
    - cron: "40 14 * * *"

permissions:
  contents: write

concurrency:
  group: nasdaq-bb-qqq-len60-k2-logclose
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install deps
        run: |
          python -V
          pip install --upgrade pip
          pip install pandas numpy requests

      # 建議先保留：確認 runner 找得到 scripts/ 下的檔案
      # 等你確認穩了再刪掉
      - name: Debug list files
        run: |
          pwd
          echo "---- repo root ----"
          ls -la
          echo "---- scripts/ ----"
          ls -la scripts || true
          echo "---- git files (nasdaq) ----"
          git ls-files | grep -i nasdaq || true

      - name: Run Nasdaq BB monitor (QQQ + optional VXN)
        run: |
          mkdir -p nasdaq_bb_cache
          python scripts/nasdaq_bb_len60_k2_logclose.py \
            --price_ticker "qqq.us" \
            --out_dir "nasdaq_bb_cache" \
            --vxn_enable \
            --vxn_source cboe_first \
            --vxn_code VXN \
            --vxn_fred_series VXNCLS \
            --bb_len 60 \
            --bb_k 2.0 \
            --z_thresh -1.5 \
            --z_thresh_low -2.0 \
            --z_thresh_high 2.0 \
            --horizon 20 \
            --cooldown 20 \
            --quiet

      - name: Build report.md (cache only)
        run: |
          python scripts/build_nasdaq_bb_report.py \
            --cache_dir "nasdaq_bb_cache"

      - name: Commit outputs (only if changed)
        run: |
          # 只提交 nasdaq_bb_cache 的變更，避免誤 commit 其他檔案
          if [ -n "$(git status --porcelain nasdaq_bb_cache)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add nasdaq_bb_cache
            git commit -m "update: nasdaq BB(60,2) QQQ/VXN cache + report"
            git push
          else
            echo "No changes to commit."
          fi